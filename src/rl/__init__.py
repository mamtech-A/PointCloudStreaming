"""Reinforcement-learning layer: DQN ABR agent, gym-like environment, and the
shared feature/reward builder. Imports from `network_model`, never the reverse
(except `DQNABR`, which imports from here lazily)."""
